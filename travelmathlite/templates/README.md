# Project-level templates

This directory holds project-wide templates that aren't specific to any single app.

Per-app templates live under `apps/<app_name>/templates/<app_name>/` to maintain namespacing.
